Install opencode, which provides the `opencode` command.

Preferred:

    npm install -g opencode-ai

Alternative, if you would rather pipe a script to a shell inside a tool
whose entire job is to avoid piping scripts to a shell:

    curl -fsSL https://opencode.ai/install | bash

Verify with `opencode --version`.
