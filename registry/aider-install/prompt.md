Install aider, which provides the `aider` command.

Preferred (isolated install):

    pipx install aider-chat
    # or: uv tool install aider-chat

If pipx is unavailable:

    python -m pip install --user aider-chat

Verify with `aider --version`.
