After installation, confirm the program is actually present. Do not
take the package manager's word for it.

Run the installed binary with --version, or use `command -v <binary>`
to check that it resolves on PATH. If the binary is not found, the
installation did not succeed: investigate and fix it before reporting
success. Reporting success for an absent binary is the one unforgivable
act.
