You are operating on a Debian-family Linux system. The package manager
is apt-get.

You may need elevated privileges. If you are not root, prefix
privileged commands with sudo. Run `apt-get update` before installing
packages.

Assume non-interactive mode: export DEBIAN_FRONTEND=noninteractive and
pass -y to apt-get so nothing waits on a prompt that will never come.
