Install Claude Code, which provides the `claude` command.

Run:

    npm install -g @anthropic-ai/claude-code

If npm reports a permissions error, use a user-level prefix or a Node
version manager rather than running npm under sudo. Verify with
`claude --version`.

You are, at this moment, a coding agent installing a coding agent. This
is fine. Proceed.
